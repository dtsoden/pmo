# Chrome Extension Build-Time Environment Variables
# Copy this file to .env and update with your values
# These are injected at build time by Vite (NOT runtime in browser)
# Only variables prefixed with VITE_ are exposed to the extension code

# Extension API Key - must match backend EXTENSION_API_KEY in root .env
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
VITE_EXTENSION_API_KEY=your-extension-api-key-here-must-match-backend

# Backend URL - Where extension connects to API (must be absolute URL)
# Development: http://localhost:7600
# Production: https://pmoservices.yourdomain.com
VITE_EXTENSION_BACKEND_URL=http://localhost:7600

# Frontend URL - Where extension links to web app
# Development: http://localhost:7620
# Production: https://pmo.yourdomain.com
VITE_EXTENSION_FRONTEND_URL=http://localhost:7620
