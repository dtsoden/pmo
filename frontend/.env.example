# Frontend Build-Time Environment Variables
# Copy this file to .env and update with your values
# These are injected by Vite at build time (NOT runtime)
# Only variables prefixed with VITE_ are exposed to browser code

# API Base Path - Where frontend connects to backend API
# Development: /api (proxied by Vite to localhost:7600)
# Production: /api (proxied by nginx/cloudflare) OR full URL like https://api.yourdomain.com/api
VITE_API_BASE=/api

# WebSocket Base Path - Where frontend connects for real-time updates
# Development: / (proxied by Vite to localhost:7600)
# Production: / (proxied by nginx/cloudflare) OR full URL like wss://api.yourdomain.com
VITE_WS_BASE=/

# Backend URL for Extension Setup - Extension needs absolute URL (no proxy)
# Development: http://localhost:7600
# Production: https://api.yourdomain.com
VITE_EXTENSION_BACKEND_URL=http://localhost:7600

# Frontend URL for Extension - Extension uses this to link back to web app
# Development: http://localhost:7620
# Production: https://yourdomain.com
VITE_EXTENSION_FRONTEND_URL=http://localhost:7620
